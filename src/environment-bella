
# arch doesn't set locale globally anymore?
export LANG=en_US.UTF-8

export MOST_EDITOR=/usr/bin/nvim

export EMAIL=`echo -n '"Sreanaqb Y. Pnavmb" <syp@zhevnaqer.pbz>' | tr n-za-mN-ZA-M a-zA-Z`

export PYTHONDOCS="/usr/share/doc/python/html/"

# Avoid Wesnoth taking sound for itself
export SDL_AUDIODRIVER="alsa"

export TERM=rxvt-unicode

# Always use IPython for shell_plus
export SHELL_PLUS="bpython"

export BROWSER='/usr/lib/chromium/chromium'

export ANDROID_HOME="${HOME}/android-studio"
export JAVA_HOME='/usr/lib/jvm/java-8-jdk'

export PATH=~/bin/private:~/bin/public:~/bin/alien:~/myPython3/bin:/usr/lib/git-core:/home/flc/local/geogig/bin:/usr/local/bin:$ANDROID_HOME/tools/:$ANDROID_HOME/tools/bin:$ANDROID_HOME/platform-tools/:/opt/grass/bin/:$PATH

if [[ ${EUID} -ne 0 ]] ; then
  export PS1='$(gbt $?)'
fi
