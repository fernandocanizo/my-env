*.ignore
# mc file is added to have an initial configuration for mc to my liking
# but since this file could change on each mc usage, I ignore it
app/mc/panels.ini

# Have an initial configuration for Midnight Commander, but ignore changes
# afterwards since `mc` makes changes as you use it
app/mc/ini

git/gitk
